export const Header = () => {
    const headerText = "New Albums & Singles";
    const headerUnderText = "By Ester & Ida"
    
    return (
        <header> 
            <h1>{headerText}</h1>
            <h2>{headerUnderText}</h2>
        </header>
    )
}
